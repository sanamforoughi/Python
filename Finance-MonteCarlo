import pandas_datareader as web
import pandas as pd
import datetime as dt
import numpy as np
import matplotlib.pyplot as plt
from matplotlib import style

style.use('ggplot')

start = dt.datetime(2017, 01, 03)
end = dt.datetime(2018, 01, 06) # you can change this to prove that it works by testing it with last week's price 

prices = web.DataReader('HZNP', 'yahoo', start, end)['Close']
returns = prices.pct_change()

last_price = prices[-1]

#Number of Simulations
num_simulations = 1000
num_days = 252 # number of days your projection is more

simulation_df = pd.DataFrame()

for x in range(num_simulations):
    count = 0
    daily_vol = returns.std()
    
    price_series = []
    
    price = last_price * (1 + np.random.normal(0, daily_vol))
    price_series.append(price)
    
    for y in range(num_days):
        if count == 251:
            break
        price = price_series[count] * (1 + np.random.normal(0, daily_vol))
        price_series.append(price)
        count += 1
    
    simulation_df[x] = price_series

     # print np.mean(simulation_df[x])

   
    print(simulation_df[x])
    print(price_series)#this hurts a little
    print("this is the mean")
    print np.average(price_series)
    
fig = plt.figure()
fig.suptitle('Monte Carlo Simulation: HZNP')
plt.plot(simulation_df)
plt.axhline(y = last_price, color = 'r', linestyle = '-')
plt.xlabel('Day')
plt.ylabel('Price')
plt.show()
